define(["toastr", "utils"],
    function(toastr, utils) {
        "use strict";

        var options = {
            timeOut: 5000,
            closeButton: true
        };

        return {
            //connect: function (name) {
            //    toastr.info(utils.format('Пользователь {0} входит в страницу', [name]), options);
            //}
        };
    }
);